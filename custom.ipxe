#!ipxe

# 定义 repo 变量（替换为实际仓库，例如 VzLinux 8 的镜像）
set repo https://repo.virtuozzo.com/vzlinux/8/BaseOS/x86_64/os/

# 设置信任策略：强制要求信任（--permanent），但允许未签名图像执行（--allow）
# 如果想严格模式，去掉 --allow；否则下载会因无签名而失败
imgtrust --permanent --allow

# 加载内核（带参数）
kernel https://ftp.orgv.eu/download.php?file=vmlinuz inst.repo=${repo}

# 加载 initrd（独立命令）
initrd https://ftp.orgv.eu/download.php?file=initrd.img

# 启动引导
boot
